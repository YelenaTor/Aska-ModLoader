<mah:MetroWindow x:Class="ModManager.DesktopUI.Views.DependencyErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="clr-namespace:ModManager.DesktopUI.Views"
        mc:Ignorable="d"
        Title="Dependency Requirements Not Met" 
        Height="400" Width="550"
        WindowStartupLocation="CenterOwner"
        GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="⚠" FontSize="32" Foreground="IndianRed" VerticalAlignment="Center" Margin="0,0,15,0" />
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Cannot Enable Mod" FontSize="18" FontWeight="Bold" />
                <TextBlock x:Name="ModNameText" Text="Mod Name" FontSize="14" Foreground="Gray" />
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="1" 
                   Text="The following dependencies must be resolved before this mod can be enabled:" 
                   TextWrapping="Wrap" 
                   Margin="0,0,0,10" />

        <Border Grid.Row="2" BorderThickness="1" BorderBrush="#444" CornerRadius="4" Background="#252525">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="DependencyList" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel>
                                    <TextBlock Text="{Binding Id}" FontWeight="SemiBold" FontSize="14" Foreground="White" />
                                    <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="IndianRed" />
                                </StackPanel>
                                
                                <TextBlock Grid.Column="1" 
                                           Text="{Binding MinVersion, StringFormat='v{0}+'}" 
                                           VerticalAlignment="Center" 
                                           Foreground="LightGray" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Close" 
                    Width="100" 
                    Padding="10,5"
                    Click="Close_Click"
                    Style="{StaticResource MahApps.Styles.Button.Flat}" />
        </StackPanel>
    </Grid>
</mah:MetroWindow>
